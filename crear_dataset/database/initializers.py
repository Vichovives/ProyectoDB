# Conexion a DB
import psycopg2

"""
def DBInstance():
    try:
        conn = psycopg2.connect(
            host=host,
            database=database,
            user=user,
            password=password,
            port=port
        )
        return conn
    except psycopg2.Error as err:
        print("Error al conectar la base de datos: ", err)
        return None
"""
